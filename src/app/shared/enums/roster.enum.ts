export enum ROSTER_ACTION {
  DRAFT = 'draft',
  APPROVE = 'approve',
  REVIEW = 'review',
  REJECT = 'reject',
  CANCEL = 'cancel',
  CREATE = 'create',
  UPDATE = 'update',
}

export enum CalendarControl {
  PREVIOUS = 'P',
  TODAY = 'T',
  NEXT = 'N',
}
